# Anthropic API key for the "Ask about Dike" chat (server-side only).
# Get one at https://console.anthropic.com/ -> API Keys.
# Add the same key to Vercel: Project -> Settings -> Environment Variables.
ANTHROPIC_API_KEY=
